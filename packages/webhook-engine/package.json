{
  "name": "@fiber-merchantops/webhook-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Webhook wire protocol: HMAC-SHA256 signing and verification, retry schedule math, payload envelope, and single-attempt HTTP delivery.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fiber-merchantops/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
