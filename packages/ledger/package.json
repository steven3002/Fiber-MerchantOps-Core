{
  "name": "@fiber-merchantops/ledger",
  "version": "0.1.0",
  "private": true,
  "description": "Append-only merchant ledger event model: type registry and insert-ready event factory.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fiber-merchantops/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
